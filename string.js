//regex

const message = "Hay Man";
const updatedMessage = message.replaceAll("ay", "tt");
const updatedMessage1 = message.replace("a", "e");

console.log(updatedMessage);
console.log(updatedMessage1);
